XMLPOrts

Format - XML / Fixed / Variable Text
Direction = Import / Export / Both

FieldDelimiter
FieldSeparator

<root>
   <customer>  -> Element
      <no>
      <name>
      <city country = "BE">Brussels </city>    city i an element country an attribute
  </customer>
</root>


------------

Permission Sets
Normally as developers we have SUPER Permission Sets but the users don't have that's why we need to give someone permissions on your app.

PermissionSet - Object

or VsCode > Command Pallette > Generate Permission Set aL containing objects ( before the option was only for PermissionSet.XML)

Web Client - Permission Set We have options Yes / Indirect.

Indirect Permissions we use the lowercase in our permissionsets file.

tabledata "Egg Production Header" = RMID will be changed to tabledata "Egg Production Header" = rmid
https://learn.microsoft.com/en-us/dynamics365/business-central/dev-itpro/developer/devenv-permissionset-composing


check the latest table for an example ( Could have the same situation as a question on the exam).


------------------------
Events  

Now IsHandled design pattern

We can susbribe to 
Table
Codeunit
Page 
Query ( only OnPre)
Report
XmlPort

Subriber to tables and not pages ( UI).
This is the only way to change business logic.

We can create CUs with our own events
Example
Table                  ---> var that point to table
   fields
   methods ---> Business Logics

Events Recorder


Event Publisher (2 types)
   Integration - no such promise
   Business - contains a promise that will never change or break. ISV


Example on KandyToyds
 [IntegrationEvent(false, false)]
    local procedure KandyOnCheckShippingWorksheetLineFulfilmentStatusAllowed(var EVSAVSShippingWorksheetLine: Record EVS_AVS_ShippingWorksheetLine; var AllowedLine: Boolean)
    begin
    end;


-----------------------

USER ASSISTANCE MODEL
(SSASIFICATION) - Avoid support or intervention., busing other techniques so they can sort the issues.
PPrinciples
Get Started
Get Unblocked ( tootips answers mst wimmediate questions and actions to.)
Learn more  (Help menu and tooltips provide context-sensitive links to Help articles)


-----

Work with essential development standards 
(Deign Patters)
Crerating tables ( master, supplemental subsidary, regiger, journal, ledger, document, setup)


3 types of tables
Configuration (Master , Setup, Subsidiary)
Transaction tables ( Documents, Journals)
Posted transaction ( Document History, Ledger Entry - Register)

Setup ( Code PK) - Singleton Design Pattern
PK is always empty
Data Type (PK) Code. We use Code because it can be empty ( Integer cannot be empty).

Example VAT Setup Page to create a record if it's empty
   trigger OnOpenPage()
    var
        VATReportingDateMgt: Codeunit "VAT Reporting Date Mgt";
    begin
        IsVATDateEnabled := VATReportingDateMgt.IsVATDateEnabled();
        if not Rec.Get() then begin
            Rec.Init();
            Rec.Insert();
        end;
    end;


No Series 
NoSeriesMgt.InitSeries (Insert)

on the nerw ( Business Foundation)
No.Series.AreRelated and No. Series.GetNextNo

-----------

Access Modifiers
Internal - object field can be accessed only by code in the same module not from anoter module
local - same table or table extension
protected - the field can be accessed only by code in the same table
public - object ior fieldcan be accessed by code same module or other modules

Access modifiers are only taken into consideration at compile time.

----------

Test Toolkit 

Subtype - Test 
Test functions ( all of them will be executed)
Test procedure - test should be sucessful if an raise an error.

TryFunction.

Methods on a Test Codeunit
Test method - you use the test methods that include AL code that tests the business logic in the application where each method covers a transaction.
Handler method - to automate tests by handling instancers when iser interaction is required by the code that is being tested by the test method. the handler test method is run nstead of  requested UI. the handler method should sumart the user interaction for test case such as validating messages, making selections .
Normal method - same design 

In a test codeunit, the next tests still continue to run, even if a method failed.

The TransactionModel can be set to:

AutoCommit: The AutoCommit setting is the default value. A commit is issued at then of the test method. If an error occurs during the test method, then the transaction is rolled back. Even if you catch the error with an AssertError statement.

AutoRollback: The transaction is always rolled back after test execution. If a Commit method is used during a test, then that test will fail with an error.


https://learn.microsoft.com/en-us/training/modules/introduction-test-automation/2-test-applications



None: Primarily used with TestPages. The Test method itself doesn't use a write transaction to the database, but the test method interacts with a TestPage. This TestPage has its own write transactions that are committed to the database if no errors occur. Otherwise they're rolled back at the end of the transaction.

ASSERTERROR Keyword
You use AssertError statements in test methods to test how your application behaves under failing conditions. These statements are called positive and negative tests. The AssertError keyword specifies that an error is expected at run time in the statement that follows the AssertError keyword.

If a simple or compound statement that follows the AssertError keyword causes an error, then execution successfully continues to the next statement in the test method. You can get the error text of the statement by using the GETLASTERRORTEXT Method.

If a statement that follows the AssertError keyword doesn't cause an error, then the AssertError statement causes the following error and the test method that's running produces a FAILURE result.











---------------


Telemetry

Using Session.LogMessage in AL code is the appropriate method for capturing custom telemetry events, providing developers control over event structure

To set up telemetry for a Microsoft Dynamics 365 Business Central extension, creating a Microsoft Application Insights resource in Azure is essential to collect telemetry data. Adding the connection string from this resource to the app.json file of the extension enables data transmission. Configuring telemetry settings in the app.json file is necessary but not sufficient on its own, and using Azure Monitor is for monitoring purposes after data collection.




